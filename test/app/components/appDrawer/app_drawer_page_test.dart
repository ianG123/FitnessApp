import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:prototype/app/components/appDrawer/app_drawer_page.dart';

void main() {
  testWidgets('AppDrawerPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AppDrawerPage(title: 'AppDrawer')));
    //  final titleFinder = find.text('AppDrawer');
    //  expect(titleFinder, findsOneWidget);
  });
}
