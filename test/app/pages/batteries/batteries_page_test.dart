import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:prototype/app/pages/batteries/batteries_page.dart';

void main() {
  testWidgets('BatteriesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(BatteriesPage(title: 'Batteries')));
    //  final titleFinder = find.text('Batteries');
    //  expect(titleFinder, findsOneWidget);
  });
}
