import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:prototype/app/modules/homedashboard/homedashboard_controller.dart';
import 'package:prototype/app/modules/homedashboard/homedashboard_module.dart';

void main() {
  initModule(HomedashboardModule());
  // HomedashboardController homedashboard;
  //
  setUp(() {
    //     homedashboard = HomedashboardModule.to.get<HomedashboardController>();
  });

  group('HomedashboardController Test', () {
    //   test("First Test", () {
    //     expect(homedashboard, isInstanceOf<HomedashboardController>());
    //   });

    //   test("Set Value", () {
    //     expect(homedashboard.value, equals(0));
    //     homedashboard.increment();
    //     expect(homedashboard.value, equals(1));
    //   });
  });
}
