import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:prototype/app/modules/homedashboard/homedashboard_page.dart';

void main() {
  testWidgets('HomedashboardPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(HomedashboardPage(title: 'Homedashboard')));
    //  final titleFinder = find.text('Homedashboard');
    //  expect(titleFinder, findsOneWidget);
  });
}
